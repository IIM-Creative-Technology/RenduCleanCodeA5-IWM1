{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "dev": "nitropack dev",
    "build": "nitropack build"
  },
  "devDependencies": {
    "nitropack": "^1.0.0",
    "vitest": "^0.25.2"
  }
}